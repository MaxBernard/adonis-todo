<textarea
  class="shadow appearance-none border rounded w-full py-2 px-3 text-grey-darker leading-tight"
  id="{{ name }}"
  name="{{ name }}"
  rows="{{rows}} | 8" cols="{{cols}} | 30"
>{{ value }}</textarea>
